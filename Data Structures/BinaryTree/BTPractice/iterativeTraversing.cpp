#include<bits/stdc++.h>
using namespace std;
#define ll long long int

class TreeNode{
public:
    int val;
    TreeNode* left;
    TreeNode* right;

    TreeNode(int val){
        left = nullptr;
        right = nullptr;
    }
};


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
	
    TreeNode root;
    cin>>root;
    while()

    return 0;
}